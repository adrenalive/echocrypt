1. Lazy Streaming: Replaced eager .read() with a generator-based pipeline, enabling processing of files larger than system RAM. 2. Unified Byte-Stream Normalization: String literals and files are wrapped in io.BytesIO or file handles to provide a consistent binary interface. 3. Consistent Cycle State: The XOR generator maintains internal state across chunks to ensure key-alignment integrity. 4. Resource Safety: Implemented context-managed output redirection via conditional expression and the with-statement.